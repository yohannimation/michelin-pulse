# Application Strava API — https://www.strava.com/settings/api
# "Authorization Callback Domain" doit être : localhost (en dev)
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=

# Secret de chiffrement de la session (cookie). Générer avec :
#   openssl rand -base64 32
SESSION_SECRET=
